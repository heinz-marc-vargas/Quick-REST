<%= form_for(@api_page) do |f| %>
  <% if @api_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_page.errors.count, "error") %> prohibited this api_page from being saved:</h2>

      <ul>
      <% @api_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :published_on %><br />
    <%= f.datetime_select :published_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
